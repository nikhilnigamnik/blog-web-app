import React from 'react'
import { useParams } from 'react-router-dom'

const BlogDeatils = () => {
    const {id} = useParams()
  return (
    <div>BlogDeatils</div>
  )
}

export default BlogDeatils